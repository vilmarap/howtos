## Como desabilitar globalmente o git gpg signing

```bash
git config --global commit.gpgsign false 
```

## Como desabilitar o git gpg signing em apenas um repositório
```bash
git config commit.gpgsign false
```